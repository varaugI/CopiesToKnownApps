# Phase 0 runs entirely with mock data in the browser.
VITE_MOCK_MODE=true

# Reserved for the typed API boundary introduced in a later phase.
VITE_API_BASE_URL=http://localhost:3000/api/v1
VITE_SOCKET_URL=http://localhost:3000
